import { ProductoNutricional } from './producto-nutricional.entity';

describe('ProductoNutricional', () => {
  it('should be defined', () => {
    expect(new ProductoNutricional()).toBeDefined();
  });
});
